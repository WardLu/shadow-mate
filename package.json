{
  "name": "shadow-mate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "node scripts/check.mjs",
    "security:check": "node scripts/security-check.mjs",
    "verify": "npm run security:check && npm run check && npm run build && node scripts/check-build.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.111.0"
  },
  "devDependencies": {
    "supabase": "2.111.0",
    "vite": "8.2.0"
  },
  "engines": {
    "node": ">=22"
  }
}
