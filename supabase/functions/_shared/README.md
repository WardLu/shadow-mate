# Shadow Mate Edge shared bundle

This directory is intentionally empty of runtime modules for `shadow-mate-functions-v1`.
The current Shadow Mate Edge Functions import only provider packages and do not share
application helpers. The directory remains part of the declared bundle so the shared
local Edge generation can verify and copy a stable, owned source tree.
